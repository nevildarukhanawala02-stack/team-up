# Team Up website — environment variables
#
# Nothing is required for local dev or a basic Railway deploy — the app
# builds and serves as a static React site behind a small Express server.
#
# PORT is provided automatically by Railway in production; set it yourself
# only for local testing if you want something other than 3000.
# PORT=3000
